# Core dependencies
yt-dlp>=2023.7.6
ffmpeg-python>=0.2.0
PyYAML>=6.0
tqdm>=4.64.0

# Audio processing
numpy>=1.22.0,<2.2.0

# ML dependencies (optional, install separately for GPU support)
# For CUDA support, install: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# For CPU-only: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0  
torchaudio>=2.0.0

# ASR model support (requires PyTorch)
nemo_toolkit[asr]>=2.4.0
